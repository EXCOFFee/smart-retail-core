# SMART_RETAIL Admin Web - Environment Variables

# API URL (backend)
# En desarrollo, Vite proxy maneja /api -> localhost:3000
# En producción (Vercel), vercel.json maneja el rewrite a Railway
VITE_API_URL=/api

# Producción: URL directa del backend en Railway (si no usar rewrites)
# VITE_API_URL=https://backend-production-bd9a.up.railway.app/api/v1
